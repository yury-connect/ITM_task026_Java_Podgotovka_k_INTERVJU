# Как разрешить циклическую зависимость при использовании Dependency Injection (DI)

---
(актуально для Spring Framework)
## 🥇 1. Правильный способ — **разорвать архитектурный цикл**

**Самый правильный и актуальный подход**
### 💡 Принцип:
Если `A → B` и `B → A`, значит нарушен SRP или слой.
### 🔧 Решения:
- Вынести общую логику в `C`    
- Использовать интерфейс + событийную модель    
- Разделить write/read ответственность    

📌 Психо-лайфхак:  
Цикл почти всегда сигнал о плохой границе ответственности.

---
## 🥈 2. Внедрение через `@Lazy`

**Когда быстро нужно починить**
```java
public class A {  
   private final B b;  
  
   public A(@Lazy B b) {  
       this.b = b;  
   }  
}
```
🔹 Создание бина откладывается  
🔹 Внедряется proxy

⚠ Минус: маскирует архитектурную проблему.

---
## 🥉 3. Setter / Field Injection вместо Constructor

**Работает, но хуже**

Spring может:
1. Создать A    
2. Создать B    
3. Вколоть зависимости позже    

⚠ Минус:
- Нарушает immutability    
- Усложняет тестирование    

---
## 4. Использование `ObjectProvider` / `Provider`

Позволяет получать бин по требованию:
```java
private final ObjectProvider<B> bProvider;
```

```java
bProvider.getObject();
```
🔹 Ленивая инициализация  
🔹 Контролируем момент создания

---
## 5. `@PostConstruct` + отложенная логика

Иногда цикл не в DI, а в логике инициализации.
🔹 Создаём бины  
🔹 Связываем их после старта

---

## 6. Событийная модель (ApplicationEvent)

Вместо прямого вызова:
```
A → публикует событие  
B → слушает
```

→ нет прямой зависимости.

---
# ⏳ Как разнести бины во времени создания?

### Инструменты:

| Инструмент                     | Что делает               |
| ------------------------------ | ------------------------ |
| `@Lazy`                        | откладывает создание     |
| `ObjectProvider`               | создаёт по запросу       |
| `@DependsOn`                   | задаёт порядок           |
| Prototype scope                | создаёт каждый раз новый |
| `ApplicationContext#getBean()` | ручное получение         |

---
# 🧠 Как быстро принимать решение

1️⃣ Это архитектурная ошибка? → рефакторить  
2️⃣ Нужно быстро починить? → `@Lazy`  
3️⃣ Нужно контролировать момент? → `ObjectProvider`  
4️⃣ Нужно убрать прямую связь? → события

---
[к вопросам](Documents/TASKs/Собесы_АНАЛИТИКА/2026-02-27_Айтеко/Вопросы)
