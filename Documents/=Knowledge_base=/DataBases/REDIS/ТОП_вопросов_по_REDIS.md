### Архитектура и базовые концепции

1. **Почему Redis считается однопоточным (Single-threaded) и за счет каких механизмов (I/O Multiplexing) он достигает высокой производительности?**
    
2. **Какие структуры данных поддерживает Redis (String, Hash, List, Set, Sorted Set, Streams, HyperLogLog) и для каких бизнес-задач применяется каждая из них?**
    
3. **В чем ключевые архитектурные и функциональные отличия Redis от Memcached?**
    

### Паттерны кэширования и работа с памятью

4. **Какие основные стратегии работы с кэшем существуют (Cache-Aside, Read-Through, Write-Through, Write-Behind) и в каких случаях они применяются?**
    
5. **Как устроены механизмы вытеснения данных (Eviction Policies: LRU, LFU, Volatile/Allkeys) и удаление ключей по истечении TTL (Active/Passive expiration)?**
    
6. **Чем отличаются механизмы сохраняемости (Persistence) RDB (snapshots) и AOF (append-only log), и какой компромисс выбрать для баланса производительности и надежности?**
    
7. **Что такое «Big Keys», чем они опасны для однопоточного Redis и как их правильно находить и удалять (`UNLINK` вместо `DEL`)?**
    

### Проблемы и уязвимости кэша (Cache Pitfalls)

8. **Что такое Cache Avalanche (лавина) и Cache Penetration (пробивание кэша), и какими архитектурными решениями они предотвращаются?**
    
9. **Что такое Cache Breakdown и Cache Stampede (Thundering Herd) и как избежать массового обращения клиентов к БД при «протухании» горячего ключа?**
    

### Атомарность, транзакции и скрипты

10. **Как работают транзакции в Redis (`MULTI`, `EXEC`, `WATCH`, `DISCARD`) и почему в Redis нет полноценного механизма отката (Rollback), как в реляционных БД?**
    
11. **Зачем используются Lua-скрипты в Redis и как они обеспечивают атомарное выполнение комплексной бизнес-логики?**
    

### Отказоустойчивость и масштабирование

12. **В чем разница между схемами организации Redis: Master-Replica, Redis Sentinel (High Availability) и Redis Cluster (Sharding, Hash Slots)?**
    
13. **Чем отличается паттерн шины сообщений Pub/Sub от решения на базе Redis Streams (наличие персистентности, подтверждение доставки, Consumer Groups)?**
    

### Специфика Java / Spring Ecosystem

14. **Как корректно реализовать распределенную блокировку (Distributed Lock) в Java с использованием Redis (проблемы `SETNX` + TTL, механизм Redlock, библиотека Redisson)?**
    
15. **В чем разница между Java-клиентами Jedis и Lettuce (синхронность/потокобезопасность/асинхронность), и где применяется Redisson?**
    
16. **Как устроена интеграция со Spring Boot через Spring Data Redis (аннотация `@Cacheable`, настройки TTL, выбор сериализатора: Jackson, JDK, String)?**
    
17. **Как реализовать паттерн Rate Limiter (ограничение частоты запросов) средствами Redis (алгоритмы Sliding Window / Token Bucket)?**

---
