G1 (Garbage-First) — это серверный сборщик мусора (GC), который был разработан для приложений с большими объемами памяти и высокими требованиями к предсказуемости времени отклика. Начиная с Java 9, он является сборщиком мусора по умолчанию в виртуальной машине HotSpot [](https://doc.ruscomtech.ru/index.php?title=%D0%A1%D0%B1%D0%BE%D1%80%D1%89%D0%B8%D0%BA_%D0%BC%D1%83%D1%81%D0%BE%D1%80%D0%B0_G1_-_Java_9&oldid=100)[](https://dev.java/learn/jvm/tool/garbage-collection/java-specifics/#garbage-collection-process)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/). Его главная цель — баланс между высокой пропускной способностью и низкими задержками, обеспечивая паузы «stop-the-world» заданной продолжительности (по умолчанию 200 мс) [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/G1.html)[](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/).

Вот инфографика, которая наглядно показывает место G1 среди других сборщиков мусора в Java [](https://dev.java/learn/jvm/tool/garbage-collection/java-specifics/#garbage-collection-process).

```mermaid
flowchart LR
    subgraph A[Типы Garbage Collector в Java]
    direction TB
    S["<b>Serial GC</b><br>для клиентских<br>однопоточных сред"]
    P["<b>Parallel GC</b><br>Максимальная<br>пропускная способность"]
    CMS["<b>CMS</b><br>Минимизация пауз<br>(устарел)"]
    G1["<b>G1 GC</b><br>Баланс задержки и<br>пропускной способности"]
    Z["<b>ZGC</b><br>Сверхнизкие задержки<br>для огромных куч"]
    end
    style G1 fill:#2a76c6,stroke:#333,stroke-width:2px,color:#fff
```

Давайте детально разберем, как устроен и работает G1.

### 🏗️ Архитектура: Регионы вместо поколений

Главное новшество G1 — отказ от физически непрерывного деления кучи на поколения (Eden, Survivor, Old). Вместо этого вся куча разбивается на множество независимых областей одинакового размера — **регионов (regions)** [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/G1.html)[](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.slideshare.net/slideshow/codefest-2012-g1-hotspot-jvm/12500343).

- **Размер регионов:** Обычно от 1 до 32 МБ (зависит от размера кучи) [](https://www.slideshare.net/slideshow/codefest-2012-g1-hotspot-jvm/12500343)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/)[](https://examples.javacodegeeks.com/java-development/core-java/java-9-default-garbage-collector-tutorial/).
    
- **Логические поколения:** Каждый регион не привязан жестко к какому-то поколению. В начале работы приложения региону может быть присвоена роль Eden (новые объекты). Если объект выживает после сборки, он копируется в регион, которому назначена роль Survivor, а затем и Old. Это позволяет G1 очень гибко управлять памятью и динамически менять размеры поколений [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/)[](https://examples.javacodegeeks.com/java-development/core-java/java-9-default-garbage-collector-tutorial/).
    
- **Крупные объекты (Humongous Objects):** Если объект занимает больше половины размера региона, он считается "гигантским" и размещается в специальных непрерывных регионах `Humongous`. Работа с такими объектами неэффективна, и их следует избегать, так как они могут привести к фрагментации памяти и преждевременным полным сборкам (Full GC) [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/)[](https://examples.javacodegeeks.com/java-development/core-java/java-9-default-garbage-collector-tutorial/).    

### ⚙️ Как это работает: От молодого до смешанных сборок

Процесс сборки мусора в G1 состоит из нескольких ключевых этапов [](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/)[](https://examples.javacodegeeks.com/java-development/core-java/java-9-default-garbage-collector-tutorial/).

- **Молодая сборка (Young Collection):** Этот этап происходит, когда все регионы Eden заполнены. Приложение приостанавливается (stop-the-world), но пауза обычно очень короткая. Все живые объекты из Eden и Survivor копируются (эвакуируются) в новые регионы Survivor или Old. Мертвые объекты просто оставляются, а освободившиеся регионы возвращаются в пул свободных [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/)[](https://examples.javacodegeeks.com/java-development/core-java/java-9-default-garbage-collector-tutorial/).
    
- **Параллельная маркировка (Concurrent Marking):** G1 не ждет, пока старое поколение заполнится целиком. Когда заполненность кучи достигает порога (`-XX:InitiatingHeapOccupancyPercent`, по умолчанию 45%), запускается фоновый процесс маркировки живых объектов во всей куче. Важно, что этот процесс выполняется **параллельно** с работой приложения, что минимизирует задержки [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/G1.html)[](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://issues.apache.org/jira/secure/attachment/12970104/HBASE-15898-001.PATCH).
    
- **Смешанная сборка (Mixed Collection):** После завершения маркировки G1 точно знает, в каких регионах старого поколения больше всего мусора. Следующие несколько сборок будут смешанными: они собирают не только молодое поколение, но и несколько самых "мусорных" регионов из старого. Это позволяет очищать старое поколение по частям, не останавливая приложение надолго [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/).
    
- **Полная сборка (Full GC):** Это аварийный режим, которого стараются избегать. Он возникает, если G1 не успевает освобождать память, и приложение пытается выделить объект в переполненной куче. Full GC останавливает приложение и выполняет одно- или многопоточную (в зависимости от версии Java) дефрагментацию всей кучи, что может вызывать очень долгие паузы [](https://www.cloudera.com/blog/technical/cdh6-3-hbase-g1-gc-tuning-with-jdk11.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/).    

### 📝 Важные внутренние механизмы

Для эффективной работы G1 использует несколько служебных структур:

- **Remembered Sets (RSet):** Каждый регион хранит специальную таблицу (RSet), в которой записано, какие объекты из _других_ регионов ссылаются на объекты внутри _этого_ региона. Это позволяет G1 собирать отдельные регионы, не сканируя всю кучу в поисках ссылок на них [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.slideshare.net/slideshow/codefest-2012-g1-hotspot-jvm/12500343)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/).
    
- **Snapshot-At-The-Beginning (SATB):** Это алгоритм, гарантирующий, что во время параллельной маркировки все живые объекты будут учтены. G1 делает "снимок" состояния кучи на момент начала маркировки и считает все объекты в этом снимке живыми, даже если позже они станут недостижимыми. Это создает эффект "плавающего мусора", который будет собран в следующий раз, но упрощает и ускоряет процесс маркировки [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.slideshare.net/slideshow/codefest-2012-g1-hotspot-jvm/12500343)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/).    

### 🚀 Настройка и мониторинг

G1 спроектирован так, чтобы требовать минимальной настройки, но понимание ключевых параметров поможет вам выжать из него максимум.

- **Ключевые параметры JVM [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/)[](https://issues.apache.org/jira/secure/attachment/12970104/HBASE-15898-001.PATCH):**
    
    - `-XX:+UseG1GC`: Включение G1 (актуально только для Java 8, т.к. в Java 9+ он по умолчанию).
        
    - `-Xms<size> -Xmx<size>`: Установка начального и максимального размера кучи.
        
    - `-XX:MaxGCPauseMillis=<N>`: **Самый важный параметр**. Целевая максимальная длительность паузы (например, 200 мс). G1 будет стараться подстроить свою работу под это значение.
        
    - `-XX:InitiatingHeapOccupancyPercent=<N>`: Процент заполнения кучи, при котором запускается параллельная маркировка (по умолчанию 45). Уменьшение значения заставляет маркировку начинаться раньше.
    
- **Признаки проблем и их решение [](https://www.cloudera.com/blog/technical/cdh6-3-hbase-g1-gc-tuning-with-jdk11.html)[](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/):**
    
    - **Полные GC (Full GC):** Если они появляются, это сигнал, что G1 не справляется. Первое, что нужно попробовать — увеличить размер кучи (`-Xmx`). Также стоит проанализировать, нет ли в приложении большого количества "гигантских объектов" (humongous allocations).
        
    - **Ошибка "To-space exhausted":** Эта ошибка в логах означает, что G1 не хватило места для копирования живых объектов во время эвакуации. Как и в случае с Full GC, наиболее вероятное решение — увеличение размера кучи [](https://www.cloudera.com/blog/technical/cdh6-3-hbase-g1-gc-tuning-with-jdk11.html).
        
- **Мониторинг:** Обязательно включайте логирование GC для анализа его работы. Это можно сделать с помощью ключей:  
    `-Xlog:gc*:file=gc.log:time,uptime:filecount=5,filesize=20m`  
    Анализ этих логов и использование JFR (Java Flight Recorder) помогут вам понять, сколько времени занимают разные фазы и как эффективно работает сборщик [](https://www.cleverence.com/articles/oracle-documentation/garbage-first-g1-garbage-collector-java-9473/).

### 💡 Когда G1 — лучший выбор?

G1 отлично подходит для приложений, которые соответствуют одному или нескольким критериям [](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/G1.html)[](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/g1_gc.html)[](https://www.cloudera.com/blog/technical/cdh6-3-hbase-g1-gc-tuning-with-jdk11.html):

- Требуется предсказуемое время отклика с паузами не более 500 мс (а часто и менее 200 мс).
    
- Размер кучи составляет от 4-6 ГБ и больше.
    
- В куче много живых данных (более 50%).
    
- Скорость выделения и продвижения объектов (allocation/promotion rate) сильно меняется со временем.
    
- Наблюдаются длительные паузы GC при использовании других сборщиков (Parallel, CMS).    

В заключение, G1 — это мощный и зрелый сборщик мусора, который эффективно работает "из коробки". Понимание его архитектуры и ключевых метрик позволит вам создавать высокопроизводительные и отзывчивые Java-приложения.

---
