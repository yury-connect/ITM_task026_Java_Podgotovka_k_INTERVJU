# Как мапятся Enum'ы?

---
## 🧩 Маппинг`Enum`в_JPA_
#### В JPA предусм. **3** осн-х способа маппинга`enum`-ов на БД:

---
## 🟢 1.`@Enumerated`<br>_Быстро, просто, без изысков_
```java
@Enumerated(EnumType.STRING)   // Сохраняет имя enum
@Enumerated(EnumType.ORDINAL) // Сохраняет порядковый номер enum
```

| **Тип**   | **Поведение**                                | **Плюсы**                                     | **Минусы**                                            |
| --------- | -------------------------------------------- | --------------------------------------------- | ----------------------------------------------------- |
| `STRING`  | Хранится строковое имя (`Enum.name()`)       | ✅ Читабельно,  <br>✅ Устойчиво к перестановке | ❌ Изменение имени ломает БД,  <br>❌ Больше места в БД |
| `ORDINAL` | Хранится порядковый номер (`Enum.ordinal()`) | ✅ Экономно по памяти                          | ❌ Хрупко: перестановка enum ломает данные             |
#### 📌_Когда использовать_:
- **STRING**— для ясности и читаемости
- **ORDINAL**— только если уверен, что enum не изменится_никогда_! 😅

### 🟡 2.`@Convert`с`AttributeConverter`(_JPA 2.1+_)<br> _Для эстетов и перфекционистов_
- Позволяет гибко контролировать, что и как сохраняется.
- Можно безопасно добавлять или переименовывать значения`enum`.
```java
@Convert(converter = YourEnumConverter.class)
```

✅ Гибкость  
✅ Поддержка *legacy*-данных  
✅ Устойчиво к изменению *enum*  
❌ Нужно писать код → 
	`GenderConverter implements AttributeConverter<Gender, String>` 
#### 📌_Когда использовать_:
- Когда надо**сохранить старый формат**
- При**рефакторинге и миграции данных**
- Когда бизнес-логика требует**особенного подхода**

### 🔴 3. Через `@PostLoad`и`@PrePersist`+`@EntityListeners`<br>_Как точная настройка тюнинговой машины_🛠️
- Альтернатива`@Convert`, но требует вспомогательного поля + логика в слушателе.

✅ Полный контроль над процессом  
✅ Поддержка сложных логик  
❌ Нужно два поля: enum и String  
❌ Может стать спагетти-кодом 🍝 если увлечься  
#### 📌_Когда использовать_:
- Если нужна логика обработки на лету
- При неочевидных связях и форматах

### 🔚 Рекомендации:

|**Цель**🚩|**Рекомендуемый способ**💌|
|---|---|
|Простота и читаемость|`@Enumerated(EnumType.STRING)`|
|Экономия места|`@Enumerated(EnumType.ORDINAL)`(_но на свой страх и риск_) 😅|
|Гибкость и надёжность|`@Convert`+`AttributeConverter`|
|Сложная кастомная логика|`@PostLoad`/`@PrePersist`+`@EntityListeners`|

---
# **Примеры:**

## **1. По умолчанию** <br>(*сохраняет`ordinal()`— числовой индекс*)
```java
public enum Status {
    NEW, IN_PROGRESS, DONE
}

@Entity
public class Task {
    @Id
    private Long id;

    @Enumerated(EnumType.ORDINAL)  // Можно не указывать (значение по умолчанию)
    private Status status;  // В БД: 0 (NEW), 1 (IN_PROGRESS), 2 (DONE)
}
```
**Минус:**Хрупкость — изменение порядка *enum*-элементов сломает существующие данные.
#### Код
```java
@Enumerated(EnumType.ORDINAL)  // или без аннотации (по умолчанию)
private Status status;  // enum: NEW, IN_PROGRESS, DONE
```

#### Что в БД

|`id`|`status`(INT)|
|---|---|
|1|`0`(NEW)|
|2|`1`(IN_PROGRESS)|
|3|`2`(DONE)|
#### Пояснение  
*JPA* сохраняет**порядковый номер**элемента enum (*начинается с 0*).  
⚠️ **Опасно**: если изменить порядок элементов в`enum`, старые данные станут некорректными!

---
## **2. Сохранение имени (*`name()`— строка*)**
```java
@Entity
public class Task {
    @Id
    private Long id;

    @Enumerated(EnumType.STRING)  // Сохраняет имя значения
    private Status status;  // В БД: "NEW", "IN_PROGRESS", "DONE"
}
```
**Плюс:**Устойчивость к изменению порядка *enum*.  
**Минус:**Занимает **больше места** в БД.
#### Код:
```java
@Enumerated(EnumType.STRING)
private Status status;  // enum: NEW, IN_PROGRESS, DONE
```

#### Что в БД:

|`id`|`status`(VARCHAR)|
|---|---|
|1|`"NEW"`|
|2|`"IN_PROGRESS"`|
|3|`"DONE"`|
#### Пояснение: 
JPA сохраняет**имя значения enum**как строку.  
✅ Безопасно: даже если порядок enum изменится, данные останутся валидными.

---
## **3. Кастомный маппинг через`@Converter`**
Если нужно сохранять enum в особом формате (*например, код или сокращение*):
```java
public enum Priority {
    HIGH("H"), MEDIUM("M"), LOW("L");
	
    private final String code;
	
    Priority(String code) {
        this.code = code;
    }
	
    public String getCode() {
        return code;
    }
}

@Converter(autoApply = true)
public class PriorityConverter implements AttributeConverter<Priority, String> {
    @Override
    public String convertToDatabaseColumn(Priority priority) {
        return priority.getCode();  // В БД: "H", "M", "L"
    }
	
    @Override
    public Priority convertToEntityAttribute(String code) {
        return Arrays.stream(Priority.values())
            .filter(p -> p.getCode().equals(code))
            .findFirst()
            .orElseThrow();
    }
}

@Entity
public class Task {
    @Id
    private Long id;
	
    private Priority priority;  // Автоматически применяется конвертер
}
```
#### Код:
```java
public enum Priority { HIGH("H"), MEDIUM("M"), LOW("L"); }

@Converter
private Priority priority;  // Конвертер преобразует в код
```
#### Что в БД:

|`id`|`priority`(VARCHAR)|
|---|---|
|1|`"H"`(HIGH)|
|2|`"M"`(MEDIUM)|
|3|`"L"`(LOW)|
#### Пояснение:
Конвертер преобразует enum в кастомное значение (*здесь — первую букву*).  
✅ Гибкость: можно хранить в БД числа, коды или сокращения.

---
## **4. Маппинг через отдельный столбец** <br>с`@PostLoad`и`@PrePersist` // *Ручное управление*
Для сложных сценариев (*например, если enum зависит от других полей*):
```java
@Entity
public class Task {
    @Id
    private Long id;
	
    @Column(name = "priority_code")
    private String priorityCode;  // В БД: "H", "M", "L"
	
    @Transient  // Не сохраняется в БД
    private Priority priority;
	
    @PostLoad
    private void postLoad() {
        this.priority = Priority.fromCode(priorityCode);
    }
	
    @PrePersist
    @PreUpdate
    private void prePersist() {
        this.priorityCode = priority.getCode();
    }
}
```
#### Код:
```java
@Column(name = "priority_code")
private String priorityCode;  // В БД: "H", "M", "L"

@Transient
private Priority priority;  // Не сохраняется в БД
```
#### Что в БД:

|`id`|`priority_code`(VARCHAR)|
|---|---|
|1|`"H"`|
|2|`"M"`|
|3|`"L"`|
#### Пояснение:
- Поле`priority`не сохраняется в БД (его аннотация`@Transient`).    
- При загрузке (`@PostLoad`) значение`priority_code`преобразуется в`enum`.    
- При сохранении (`@PrePersist`)`enum`преобразуется в`priority_code`.

---
### **Итог**

| Способ                        | Аннотация/Подход          | Хранение в БД       | Плюсы/Минусы                                        |
| ----------------------------- | ------------------------- | ------------------- | --------------------------------------------------- |
| Числовой индекс (`ordinal()`) | `@Enumerated(ORDINAL)`    | `0`,`1`,`2`       | Компактность, но хрупкость                          |
| Имя (`name()`)                | `@Enumerated(STRING)`     | `"NEW"`,`"DONE"`   | Устойчивость, но больше места                       |
| Кастомное значение            | `@Converter`              | Любое (`"H"`,`1`*) | Гибкость, но нужен конвертер                        |
| Ручное управление             | `@PostLoad`/`@PrePersist` | Любое               | Для сложных случаев, но много кода /Полный контроль |
* Пример: можно хранить как число или строку через конвертер.

---

```
***** из методички *****
* @Enumerated(EnumType.STRING) - означает, что в базе будут храниться имена Enum.
* @Enumerated(EnumType.ORDINAL) - в базе будут храниться порядковые номера Enum.  

@Enumerated(EnumType.STRING)  Однако переименование значения enum все равно нарушит работу базы данных. 
Кроме того, даже несмотря на то, что это представление данных гораздо более читаемо по сравнению 
с параметром @Enumerated(EnumType.ORDINAL), оно потребляет намного больше места, чем необходимо. 
Это может оказаться серьезной проблемой, когда нам нужно иметь дело с большим объемом данных.

Другой вариант - мы можем смапить наши enum в БД и обратно 
в методах с аннотациями @PostLoad и @PrePersist. 
@EntityListener над классом Entity, в которой указать класс, 
в котором создать два метода, помеченнх этими аннотациями.
Идея в том, чтобы в сущности иметь не только поле с Enum, но и вспомогательное поле. 
Поле с Enum аннотируем @Transient, а в БД будет храниться значение из вспомогательного поля. 
кажется неправильным иметь в сущности целых два атрибута, представляющих одно перечисление.
В JPA с версии 2.1 можно использовать Converter для конвертации Enum’а в некое его значение 
для сохранения в БД и получения из БД. Все, что нам нужно сделать, это создать новый класс, 
который реализует javax.persistence.AttributeConverter и аннотировать его с помощью @Converter 
и поле в сущности аннотацией @Convert. Мы установили @Converter(autoApply=true), 
чтобы JPA автоматически применял логику преобразования ко всем сопоставленным атрибутам типа Category. 
В противном случае нам пришлось бы поместить аннотацию @Converter непосредственно 
над полем Category у каждой сущности, где оно имеется. 
Более того, мы можем безопасно добавлять новые значения enum 
или изменять существующие, не нарушая уже сохраненные данные.
```

---
