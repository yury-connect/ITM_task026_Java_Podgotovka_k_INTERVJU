# Раздел 1. <br>JVM, JDK, JRE, JAR, class loading, build tools

```text
 1 JVM, JDK, JRE, JAR, class loading, build tools
   
1. В чем разница между JDK, JRE и JVM?
2. Какие части входят в JDK и какие из них реально используются во время разработки, сборки и запуска приложения?
3. Что происходит с Java-кодом от .java файла до выполнения инструкций на процессоре?
4. Что такое байткод и что хранится в .class файле?
5. Как работает механизм class loading в Java?
6. Какие class loader’ы существуют в стандартной JVM и за что отвечает каждый из них?
7. Чем отличаются loading, linking и initialization класса?
8. В какие моменты класс действительно инициализируется?
9. В каком порядке выполняются static-поля, static-блоки и инициализация родительских классов?
10.Что такое JAR-файл и чем обычный JAR отличается от executable JAR?
11.Какую роль играют MANIFEST.MF, Main-Class и classpath при запуске приложения?
12.Чем classpath отличается от module path, зачем появился JPMS и какие проблемы классического classpath он решает?
13.Что такое strong encapsulation модулей и как она влияет на reflection?
14.Зачем нужны системы сборки вроде Maven и Gradle, если можно компилировать код напрямую через javac?
15.Как в общих чертах работают системы сборки и что они делают помимо компиляции?
16.Как build tool разрешает транзитивные зависимости и конфликты версий?
17.Что такое annotation processing и на каком этапе сборки он работает?
18.Что делает JIT-компилятор и зачем он нужен, если код уже скомпилирован в байткод?
19.Как в общих чертах работают proling, tiered compilation, inlining, devirtualization и escape analysis?
20.Что такое JMH и почему микробенчмарки “на коленке” в Java часто дают ложные выводы о производительности?
```
---
