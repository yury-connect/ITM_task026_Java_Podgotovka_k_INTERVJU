
, что здесь не так Поругать код

```java
@Slf4j
@RestController
public class TestController {
    private final Map<Integer, Integer> map = new HashMap<>();
	 
    @PostMapping("/book")
    public boolean book(int tableId, int userId) {
        if (!map.containsKey(tableId)) {
            map.put(tableId, userId);
            log.info("table " + tableId + " booked");
            return true;
        }
        log.info("table " + tableId + " not founde");
        return false;
    }
}
```




