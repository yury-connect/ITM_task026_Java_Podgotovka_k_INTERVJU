# 🔌 WebSocket vs 📡 Server-Sent Events (SSE)

---
## 1️⃣ 🔌 WebSocket
#### **Что это:**  
Постоянное двустороннее соединение поверх TCP.
#### **Как работает:**
- Клиент делает HTTP-запрос на апгрейд протокола с `Upgrade: websocket`    
- Сервер соглашается, и соединение переключается 
	  с HTTP на протокол WebSocket *(обычно по тому же TCP-сокету)*    
- Дальше — устанавливается постоянный `full-duplex` канал    
	  Клиент и сервер отпр. сообщения друг другу независимо в любой момент  
#### **Особенности:**
- **Транспорт:** Собственный протокол (`ws://` или `wss://` для защищенного).    
- **Направление:** Полный дуплекс / ↔ Двусторонняя связь (client ⇄ server).    
- **Формат данных:** Текст и бинарные данные *(можно передавать ArrayBuffer, Blob)*.    
- **Поддержка браузерами:** Встроенный API `WebSocket`.    
- **Сложность:** Сложнее в реализации, чем SSE (нужно обрабатывать разрыв соединений, пинги/понги для keep-alive).    
- **Инфраструктура:** Не все прокси и балансировщики нагрузки умеют корректно работать с долгими WebSocket-соединениями (требуется sticky sessions или специальная настройка).
#### **Когда применять:**
- Чаты    
- Онлайн-игры    
- Биржевые котировки    
- Реальное время с высокой частотой событий    
> **Выбирайте WebSocket**, если это _интерактивное приложение_, где клиент тоже активно отправляет данные. Например, в онлайн-игре игрок нажимает кнопку "Прыжок", и это мгновенно должно улететь на сервер и другим игрокам.
#### **Плюсы:**
- Низкая задержка    
- Поддержка бинарных данных    
- Высокая производительность    
#### **Минусы:**
- Сложнее масштабировать    
- Требует `stateful`-подхода (`sticky sessions` или брокер)    
- Иногда блокируется прокси    

---
## 2️⃣ 📡 Server-Sent Events (SSE)
#### **Что это:**  
Односторонний поток данных от сервера к клиенту поверх `HTTP`.
Это технология, которая позволяет серверу отправлять данные клиенту в любой момент, как только они появляются. Клиент подписывается на поток событий
#### **Как работает:**
- Клиент открывает HTTP-соединение (особый заголовок `Accept: text/event-stream`)  
- Сервер «держит» его открытым и отправляет события/ данные в специальном текстовом формате (`data: ...\n\n`)    
- Соединение однонаправленное: **только от сервера к клиенту**.
#### **Особенности:**
- **Транспорт:** Работает поверх обычного HTTP/1.1 и HTTP/2.    
- **Направление:** Только Server-to-Client (push-уведомления).    
- **Формат данных:** Только текст (обычно JSON строки).    
- **Поддержка браузерами:** Встроенный API `EventSource`. Очень простая реализация на клиенте.    
- **Автоматическое переподключение:** Браузер автоматически переподключается, если соединение обрывается, и отправляет специальный `Last-Event-ID`, чтобы сервер мог отправить пропущенные сообщения.    
- **Ограничения:** Максимальное количество одновременных соединений к одному домену в браузере обычно ограничено (6-8 для HTTP/1.1, но меньше проблем с HTTP/2).
#### **Когда применять:**
- Нотификации    
- Обновление статуса задач    
- Ленты событий
- Уведомления на рабочем столе.    
- Мониторинг и логи (потоковые логи).    
- Любые задачи, где сервер должен постоянно слать данные клиенту, а клиенту не нужно ничего отправлять серверу кроме начального запроса.
> **Выбирайте SSE**, если вам нужно только _показывать обновления с сервера_. Например, если сервер должен обновлять график на дашборде клиента каждые 5 секунд. SSE проще, и он сам чинит соединение при обрыве интернета.
#### **Плюсы:**
- Простая реализация    
- Лучше проходит через прокси    
- Встроенная поддержка в браузере (EventSource)    
#### **Минусы:**
- Нет двусторонней связи    
- Только текст    
- Ограничения по количеству соединений в браузере    

---
## ⚖️ Ключевое сравнение

| Критерий               | WebSocket                                   | SSE                                          |
| ---------------------- | ------------------------------------------- | -------------------------------------------- |
| Направление            | ↔ двустороннее                              | → одностороннее                              |
| Протокол               | Собственный протокол (ws://, wss://)        | HTTP (обычный)                               |
| Бинарные данные        | ✅                                           | ❌                                            |
| Тип данных             | Текст и Бинарные данные                     | Только текст                                 |
| Reconnect              | Нужно реализовывать вручную                 | Встроено в браузер                           |
| Сложность              | Высокая (управление состоянием, таймаутами) | Очень низкая <br>(несколько строк кода)      |
| Поддержка брандмауэров | Может блокироваться прокси                  | Отличная (проходит везде, где проходит HTTP) |
| Масштабирование        | Сложнее (требует брокеров сообщений)        | Легче, работает поверх HTTP-серверов         |

---
## 🧠 Как быстро запомнить
- Нужен **диалог в реальном времени** → 🔌 WebSocket    
- Нужны **уведомления от сервера** → 📡 SSE    
#### Формула:
> SSE = "подписка на поток"  
> WebSocket = "живой разговор"

---

