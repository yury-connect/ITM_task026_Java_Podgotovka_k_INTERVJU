# **`@PathVariable`** и **`@RequestParam`** в Spring

---
### 🎯 **Ключевая идея**
- **`@PathVariable`** — для **идентификации ресурса** *(какая именно сущность?)*.    
- **`@RequestParam`** — для **фильтрации/сортировки/опций** *(какие данные от этого ресурса?)*.

---
### 1. `@PathVariable` *(Параметр **пути**)*
Для извлечения значений из URI шаблона. Часть самого адреса.
> - Когда значение является **обязательной частью идентификации ресурса** (например, `ID`, `username`). 
> - Для иерархических ресурсов (например, `/api/users/{userId}/orders/{orderId}`).

**Синтаксис в URL:**
> - Часть пути, без знака `?` и `=`.
> - `{someName}` в аннотации `@GetMapping` и `@PathVariable("someName")` в аргументе.
```java
@GetMapping("/users/{id}") // id - часть пути
public User getUser(@PathVariable Long id) {
    // Получить пользователя с id из пути
}
```
**Запрос:** `GET /users/123`
- `id = 123`

---
### 2. `@RequestParam` *(Параметр **запроса**)*
Для извлечения значений из параметров строки запроса (query string).
> - Для **необязательных** или **дополнительных** параметров.
> - Для **фильтрации, сортировки, пагинации, поиска**.

**Синтаксис в URL:**
> - После знака `?`, в формате `ключ=значение`.    
> - Разделяются знаком `&`.
```java
@GetMapping("/users") // Параметры идут после "?"
public List<User> getUsers(
        @RequestParam(required = false, defaultValue = "name") String sortBy,
        @RequestParam(required = false, defaultValue = "10") int limit) {
    // Вернуть пользователей, отсортированных по `sortBy`, не более `limit` штук
}
```
**Запрос:** `GET /users?sortBy=email&limit=20`
- `sortBy = "email"`    
- `limit = 20`    

**Важные атрибуты:**
- `required` (по умолчанию `true`, *т.е. обязательный*) — обязательный ли параметр.    
- `defaultValue` — значение по умолчанию, если параметр не передан (автоматически делает `required=false`).

---
### 🎓 **Простое правило выбора**

- Если значение **определяет, к какому именно ресурсу вы обращаетесь** (его уникальный идентификатор в пути) → **`@PathVariable`**.
    
- Если значение **описывает, как именно вы хотите обработать или получить данные** (фильтр, флаг, настройка) → **`@RequestParam`**.

---







